🚀 FastAPI + PostgreSQL (Neon DB) CRUD API

This project demonstrates how to build a RESTful CRUD API using FastAPI and connect it to a cloud-hosted PostgreSQL database (Neon DB).
The API supports the following database operations on a student table:

- ➕ Insert
- ✏️ Update
- ❌ Delete
- 📌 Fetch a specific record
- 📁 Save data locally into a text file (extra feature)

| Feature                              | Status |
| ------------------------------------ | ------ |
| FastAPI backend                      | ✅      |
| PostgreSQL connection using psycopg2 | ✅      |
| CRUD operations                      | ✅      |
| Swagger auto-API                     | ✅      |
| Cloud database (NeonDB)              | ✅      |

| Component         | Technology                             |
| ----------------- | -------------------------------------- |
| Backend Framework | FastAPI                                |
| Database          | PostgreSQL (NeonDB Cloud)              |
| ORM/Driver        | psycopg2 with `RealDictCursor`         |
| Testing Interface | Swagger UI (Auto-generated by FastAPI) |
| Language          | Python 3.x                             |


- 📁 Folder Structure
FastAPI-Student-DB-Project/
│
├── app.py               # Main FastAPI application
├── students.txt         # Local file storing student data
├── README.md
└── requirements.txt

🧪 How to Run the Project
1️⃣ Clone the Repository
git clone https://github.com/<your-username>/<repo-name>.git
cd <repo-name>

2️⃣ Create & Activate Virtual Environment (Optional but recommended)
python -m venv venv
source venv/bin/activate     # Mac/Linux
venv\Scripts\activate         # Windows

3️⃣ Install Required Dependencies
pip install -r requirements.txt

4️⃣ Run the FastAPI Server
uvicorn app:app --reload


🔗 Access API Documentation

Swagger UI is generated automatically:
👉 http://127.0.0.1:8000/docs

Redoc UI:
👉 http://127.0.0.1:8000/redoc


🧷 API Endpoints Summary
| Method | Endpoint                  | Description                                 |
| ------ | ------------------------- | ------------------------------------------- |
| POST   | `/student/local/save`     | Saves student record locally in a text file |
| POST   | `/student/db/insert`      | Insert new record into NeonDB               |
| PUT    | `/student/db/update`      | Update an existing student record           |
| GET    | `/student/{id}`           | Fetch a student by ID                       |
| DELETE | `/student/db/delete/{id}` | Delete a student record                     |

🗄️ Database SchemaCREATE TABLE student (
    id INT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    age INT NOT NULL
);


🧰 Tools Used

- VS Code
- PgAdmin / Neon SQL Console
- FastAPI Swagger UI
- GitHub Version Control


📌 Future Improvements

- Add GET endpoint to fetch all students
- Add JWT authentication
- Dockerize the application
- Add pagination and search filtering


🤝 Contributing

Pull requests are welcome!
For major changes, please open an issue first.

- Output
<img width="1904" height="821" alt="image" src="https://github.com/user-attachments/assets/04d970cf-1b14-458b-8cbf-2ec7f7f05832" />
